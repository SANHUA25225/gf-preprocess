# 从卫星原始数据到可用影像——GF-2 数据处理入门讲解

> 写给零基础初学者。不需要任何遥感背景，我会用生活中的例子帮你理解每一步。

---

## 一、我们拿到的是什么？

想象你买了一部新手机，拍了一张照片。手机相机会自动帮你完成对焦、曝光、白平衡、美颜……你按下快门的瞬间，这些都在后台自动完成了。

**卫星相机不一样。** 它给你的是一堆"生肉"——未经任何处理的原始数据。就像你去菜市场买了一块生猪肉，不能直接吃，需要先清洗、切块、调味、烹饪，最后才能端上桌。

高分二号（GF-2）卫星拍下的数据包含：

| 文件 | 里面是什么 | 类比 |
|---|---|---|
| `-MSS.tiff` | 多光谱影像（彩色但模糊） | 一部 800 万像素的手机拍的彩色照片 |
| `-PAN.tiff` | 全色影像（黑白但清晰） | 一部 3200 万像素的黑白相机拍的高清照片 |
| `.xml` | 拍摄参数（时间、角度、定标系数等） | 照片的 EXIF 信息（光圈、快门、ISO） |
| `.rpb` | 位置模型（卫星在哪、往哪看） | GPS 定位 + 指南针方向 |

> **关键概念**：多光谱影像有 4 个颜色通道（蓝、绿、红、近红外），但分辨率只有 3.24 米（一个像素代表地面上 3.24×3.24 米的方块）。全色影像是黑白的，但分辨率高达 0.81 米。**我们的目标就是把这俩的优点合并**——保留多光谱的颜色，同时获得全色的清晰度。

---

## 二、为什么要处理六步？

原始卫星数据有三个"毛病"：

### 毛病一：像素值是"数字"不是"物理量"

卫星记录的是一串数字（比如 327、498、612），这叫 **DN 值（Digital Number）**。它只是传感器读到的电压值，不代表真实的地表亮度。

> **类比**：温度计上显示"75"，但如果不知道单位是摄氏度还是华氏度，这个数字没有意义。我们需要把它换算成标准单位。

### 毛病二：大气层"污染"了信号

阳光穿过大气层到达地面，被地面反射后再穿过大气层回到卫星。大气层中的水汽、气溶胶、臭氧会"污染"这个信号——就像隔着一层雾霾看风景。

> **类比**：你在北京的雾霾天拍故宫，照片灰蒙蒙的。我们想把"雾霾滤镜"去掉，恢复故宫本来的颜色。

### 毛病三：影像是"歪的"

卫星拍摄时不是垂直向下看的，而且地球表面有起伏（山、谷），所以原始影像存在几何变形——山上的像素被拉长了，谷底的被压缩了。

> **类比**：用投影仪放电影，如果投影仪斜着放，画面是梯形的。我们需要把它"拉正"。

---

## 三、六步处理详解

每一步，我都会回答三个问题：**做什么？为什么做？怎么做？**

---

### 第①步：辐射定标

**做什么？** 把"数字"（DN值）换算成"物理量"（辐亮度）。

**为什么做？** 因为不同时间、不同卫星、甚至同一卫星不同传感器拍出来的 DN 值不能直接比较。辐亮度是标准化的物理量——就像统一用"米"而不是"步数"来量距离。

**怎么做？** 卫星出厂时会做一个校准实验（用标准光源照射传感器），得到一个换算公式：

```
辐亮度 = 增益系数 × DN值 + 偏移量
```

增益系数（AbsCeof）写在 xml 文件里。ENVI 软件的 `ApplyGainOffset` 工具自动读取 xml 中的系数，执行这个乘法+加法。

**输入**：MSS.tiff（4波段，DN值，16位整数）
**输出**：mux_cal.dat（4波段，辐亮度，64位浮点数，约 1.6GB）

> ⚠️ 为什么输出变大了？因为 DN 是 0~4095 的整数（2字节），辐亮度是小数（8字节），存储空间翻了 4 倍。

---

### 第①'步：补波长信息

**做什么？** 在输出的 `.hdr` 文件里补上两行波长信息。

**为什么做？** ENVI 的辐射定标工具有一个 bug——它"忘掉"了每个波段对应的波长。而下一步大气校正**必须知道波长**才能工作（不同波长的光在大气中的衰减不同）。

**怎么做？** 在 `.hdr` 文件末尾追加两行文本：
```
wavelength = {485.0, 555.0, 660.0, 830.0}
wavelength units = Nanometers
```

这四个数字是 GF-2 卫星多光谱传感器的中心波长：蓝光 485nm、绿光 555nm、红光 660nm、近红外 830nm。

---

### 第②步：大气校正（QUAC）

**做什么？** 去掉大气的影响，把"辐亮度"换算成"地表反射率"。

**为什么做？** 辐亮度是卫星收到的东西（混了大气干扰），反射率是地面真正反射的东西。有了反射率，你才能比较同一块地不同时间的影像（比如对比2020年和2025年的植被变化）。

> **类比**：辐亮度 = 你隔着雾霾看到的故宫颜色（灰蒙蒙）。反射率 = 晴空万里时故宫本来的颜色。

**怎么做？** QUAC（Quick Atmospheric Correction）算法会自动从影像中找"暗目标"（比如阴影、清水体），通过暗目标的亮度反推大气浑浊度，然后整体修正。

**输入**：mux_cal.dat（辐亮度）
**输出**：mux_quac.dat（地表反射率，32位浮点数，约 0.4GB，比输入小很多——因为32位比64位省一半空间）
**然后**：删除 mux_cal.dat（不再需要了，省磁盘）

---

### 第③步：多光谱正射校正

**做什么？** 把"歪的"多光谱影像"拉正"，并投影到标准地图坐标系（UTM）。

**为什么做？** 两个原因：
1. 消除地形引起的几何变形——山不应该看起来像被压扁了
2. 统一到地图坐标——这样才能和 GPS 轨迹、行政边界、矢量文件对齐

> **类比**：正射校正 = 把倾斜拍摄的照片矫正成"从正上方垂直看"的样子，就像 Google Earth 里的视角。

**怎么做？** 使用 RPC（有理多项式系数）模型——这是卫星自带的"镜头参数"。结合 DEM（数字高程模型，一个全球地形数据库），ENVI 的 `RPCOrthorectification` 工具算出每个像素应该在地图上的哪个位置。

**关键参数**：
- 像元大小：3.2 米（原始多光谱是 3.24 米，取整为 3.2 米）
- 重采样方法：**Nearest Neighbor**（最近邻法）——不改变原始像素值，保持光谱纯净，适合后续分类
- UTM 带号：48N 或 49N（根据经度自动计算）

**输入**：mux_quac.dat（大气校正后）
**输出**：mux_ortho.dat（正射后，约 0.55GB）
**然后**：删除 mux_quac.dat

---

### 第④步：全色正射校正

**做什么？** 把全色影像（黑白高清）也做正射校正，但**不做辐射定标**。

**为什么不做定标？** 全色影像只有一个波段（黑白），我们只需要它的几何精度（0.8米），不需要它的辐射信息。用原始 DN 值直接正射有两个好处：
1. 省掉定标步骤的 Float64 中间产物（约 17GB）
2. 不改变像素值的相对关系（融合算法内部会自动处理）

**关键参数**：
- 像元大小：**0.8 米**（原始全色是 0.81 米，取整为 0.8 米）
- 重采样方法：**Bilinear**（双线性插值）——比 Nearest Neighbor 平滑，比 Cubic 快

**输入**：PAN.tiff（原始DN值）
**输出**：pan_ortho.dat（约 2.2GB）

---

### 第⑤步：NNDiffuse 融合

**做什么？** 把 3.2 米的多光谱（有颜色）和 0.8 米的全色（有细节）融合成一张 **0.8 米的彩色影像**。

**为什么做？** 这就是整个处理链的核心目标！你可以理解为：
- 全色影像提供了**轮廓和纹理**（树叶边缘、道路边界、房屋形状）
- 多光谱影像提供了**颜色信息**（这是植被、那是水体、那是裸土）

NNDiffuse 算法把两者"结合"：从全色影像中提取空间细节，注入到多光谱影像中，结果是一张既有 0.8 米清晰度又有 4 个波段颜色的影像。

> **类比**：这就像 Photoshop 的"智能锐化"或"细节增强"——只不过它是基于物理原理的，不是艺术滤镜。

**输入**：
- 低分辨率输入：mux_ortho.dat（3.2m，4波段）
- 高分辨率输入：pan_ortho.dat（0.8m，1波段，原始DN）

**输出**：pansharp.dat（0.8m，4波段，约 9GB）
**然后**：删除 pan_ortho.dat（不再需要）

---

### 第⑥步：矢量裁剪

**做什么？** 用保护区边界（一个 .shp 文件）把融合影像切出我们关心的范围。

**为什么做？** 全幅影像 33672×32752 像素（约 9GB），但保护区只占其中一小部分（约 1-2GB）。裁剪后：
- 文件更小，处理更快
- 去掉无关区域的数据
- 聚焦研究区

**怎么做？** 分两个子步骤：
1. **矢量重投影**（ReprojectVector）：边界的坐标系可能和影像不同，先转换到同一坐标系（UTM）
2. **栅格裁剪**（SubsetRasterByVectorsDuBatch）：用边界"切蛋糕"一样把影像切出来

**输入**：pansharp.dat + boundary.shp
**输出**：clip/pansharp_boundary_utm.dat（约 1-2GB，取决于边界大小）
**然后**：删除 pansharp.dat（全幅 9GB 不再需要，保留裁剪版即可）

---

## 四、完整流程图

```
                        ┌─────────────────────┐
                        │  GF-2 L1A 原始数据   │
                        │  MSS.tiff + PAN.tiff │
                        │  + xml + rpb         │
                        └────────┬────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │   ① 辐射定标             │
                    │   DN值 → 辐亮度          │
                    │   ApplyGainOffset        │
                    │   (AbsCeof 从 xml 读取)   │
                    └────────────┬────────────┘
                                 │ mux_cal.dat (Float64, ~1.6GB)
                    ┌────────────▼────────────┐
                    │   ①' 补波长              │
                    │   .hdr 追加 2 行         │
                    │   wavelength = {...}     │
                    └────────────┬────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │   ② QUAC 大气校正        │
                    │   辐亮度 → 地表反射率     │
                    │   去除大气影响            │
                    └────────────┬────────────┘
                                 │ mux_quac.dat (Float32, ~0.4GB)
                                 │ 🗑 删 mux_cal.dat
                    ┌────────────▼────────────┐
                    │   ③ 多光谱 RPC 正射      │
                    │   3.2m, Nearest Neighbor │
                    │   纠正几何变形            │
                    └────────────┬────────────┘
                                 │ mux_ortho.dat (~0.55GB)
                                 │ 🗑 删 mux_quac.dat
                    ┌────────────▼────────────┐
                    │   ④ 全色 RPC 正射        │
                    │   0.8m, Bilinear         │
                    │   原始DN, 不定标          │
                    └────────────┬────────────┘
                                 │ pan_ortho.dat (~2.2GB)
                    ┌────────────▼────────────┐
                    │   ⑤ NNDiffuse 融合       │
                    │   颜色(3.2m) + 细节(0.8m) │
                    │   → 0.8m 彩色            │
                    └────────────┬────────────┘
                                 │ pansharp.dat (0.8m, 4波段, ~9GB)
                                 │ 🗑 删 pan_ortho.dat
                    ┌────────────▼────────────┐
                    │   ⑥ 矢量重投影 + 裁剪    │
                    │   按保护区边界切出范围     │
                    └────────────┬────────────┘
                                 │ clip/pansharp_boundary_utm.dat
                                 │ (0.8m, 4波段, ~1-2GB)
                                 │ 🗑 删 pansharp.dat
                        ┌────────▼────────────┐
                        │   ✅ 最终产品         │
                        │   0.8m 裁剪融合影像   │
                        │   可在地图/GIS中使用   │
                        └─────────────────────┘
```

---

## 五、两个重要概念

### 5.1 UTM 投影是什么？

地球是球形的，但地图是平的。把球面"摊平"到平面的方法叫**投影**。UTM（通用横轴墨卡托）是最常用的投影之一：

- 把地球切成 60 个"橘子瓣"（带），每个宽 6 度经度
- 每个带内，变形很小（适合局部区域使用）
- 编号 1-60，中国大部分在 43-53 带
- GF-2 影像在东经 107.9°~108.3°，属于 **48N 和 49N** 两个带

### 5.2 full 模式和 geo 模式有什么区别？

| | full 模式 | geo 模式 |
|---|---|---|
| 什么情况触发 | xml 中有定标系数（AbsCeof） | xml 中没有定标系数 |
| 做了哪些步骤 | ①②③④⑤⑥ 全部 | 跳过①②，只做③④⑤⑥ |
| 最终产品有什么区别 | 像素值是**地表反射率**（有物理意义） | 像素值是**原始 DN**（无量纲数字） |
| 能用来做什么 | 定量分析（植被指数、水质参数等） | 目视解译、面向对象分类 |
| 典型数据年份 | 2023-2026 年 | 2014-2022 年 |

> 为什么老数据没有定标系数？卫星传感器的灵敏度会随时间衰减，每年都需要重新校准。2022 年以前的 GF-2 数据在分发时没有附带校准系数，所以只能做几何处理，不能做辐射定标和大气校正。

---

## 六、本次处理的成果

我们对 18 景 GF-2 影像（覆盖广西金花茶保护区，2014-2026 年）完成了上述处理：

| 指标 | 数值 |
|---|---|
| 处理景数 | 18 |
| 成功 | 18（100%） |
| 最终产品大小 | 约 34 GB |
| 每景处理时间 | 约 7-10 分钟 |
| 总处理时间 | 约 3 小时 |
| 像元分辨率 | **0.8 米** |
| 波段数 | 4（蓝、绿、红、近红外） |

最终产品存放在：`D:\aaaJZ\GF2_processed\`

每景目录下有两个文件：
- `clip/pansharp_boundary_utm.dat` — 0.8 米裁剪融合影像（主要用这个）
- `mux_ortho.dat` — 3.2 米多光谱正射（光谱分析用）

---

## 七、给初学者的三点建议

1. **先理解"为什么"，再关心"怎么做"。** 每一步的存在都是为了解决一个具体问题——理解这个问题，操作就自然有意义了。

2. **不要怕报错。** 本次处理遇到了矢量裁剪参数格式错误、老数据缺字段等问题。报错信息是程序在"告诉"你哪里出了问题，学会读错误信息比写出完美的代码更重要。

3. **用 inpect_image.py 随时检查。** ENVI 的 `.dat` 文件不能直接双击打开（它不是通用的图片格式），但你可以用 `inspect_image.py` 查看它的关键信息：
   ```bash
   python inspect_image.py clip/pansharp_boundary_utm.dat
   ```
   它会在几秒内告诉你：影像多大、几个波段、分辨率多少、投影是什么、处理到了哪一步。
