# 补波长详解——为什么补、什么数据要补

> 适用于 GF-1 / GF-2 / GF-6 / GF-7 全系列 PMS/DLC 数据预处理流程中的 ①′ 步骤。
> 配套脚本实现见 `scripts/gf2_batch.py`、`scripts/gf6_batch.py` 中的 `ensure_wavelength()`。

## 一、一句话结论

**补波长是为了修复 ENVI 辐射定标工具 `ApplyGainOffset` 丢波长元数据的 bug，让下一步 QUAC 大气校正能正常运行；需要补的只有"即将进 QUAC 的多光谱辐亮度数据"——即 full 模式下定标产物 `.hdr`（PAN 全色和 geo 模式都不需要）。**

## 二、为什么要补波长？

### 1. 定标会丢波长（根因）

`ApplyGainOffset` 把原始 DN 值换算成辐亮度后，输出的 `.hdr` 文件里**不再包含 `wavelength` 元数据**——这是 ENVI 定标工具的已知行为（文档中直接称其为 bug）。实测 GF-1/GF-2/GF-6/GF-7 全系列都如此。

### 2. QUAC 强制要求波长（后果）

大气校正是基于辐射传输物理的算法：**不同波长的光在大气中的衰减程度不同**（可见光、近红外被水汽、气溶胶吸收散射的规律不一样），所以 QUAC 必须知道每个波段对应哪个波长，才能算出正确的校正量、把辐亮度换算成地表反射率。

没有波长信息时，QUAC 直接报错退出：

```
没有有效波长信息
```

### 3. 修法（在 `.hdr` 末尾追加 2 行，无需重跑任何步骤）

```
wavelength = {485.000000, 555.000000, 660.000000, 830.000000}
wavelength units = Nanometers
```

这 4 个数是 PMS/DLC 传感器官方中心波长：**蓝 485 / 绿 555 / 红 660 / 近红外 830 nm**，实测 GF-1/GF-2/GF-6/GF-7 通用。

> **类比**：就像一张照片丢了 EXIF 信息——相机型号、光圈等"参数说明"没了，后面的处理程序就不知道怎么正确对待这张照片。波长就是每个波段的核心"参数说明"。

## 三、什么样子的数据要补波长？

**判断标准：凡是"将要被 QUAC 大气校正处理"的多光谱辐亮度数据，且 `.hdr` 里没有波长信息，就需要补。**

在本工作流中具体对应：

| 数据 | 要不要补 | 原因 |
|---|---|---|
| **`ApplyGainOffset` 定标产物**（`mux_cal.dat`，Float64 辐亮度） | ✅ **必须补** | 定标丢波长，而它下一步就是进 QUAC |
| QUAC 之后的产物（`mux_quac.dat` 反射率） | ❌ 不用补 | 补过波长后 QUAC 能正常跑，且产物保留波长 |
| 正射多光谱（`mux_ortho.dat`） | ❌ 不用补 | 波长沿处理链保留下来 |
| 融合影像（`pansharp.dat`） | ❌ 不用补 | 同样保留波长（验证时可见 485–830nm ✅） |
| PAN 全色正射（`pan_ortho.dat`） | ❌ 不用补 | 1 波段黑白、不走定标和 QUAC，直接原始 DN 正射 |
| geo 模式产品（老数据，无定标系数） | ❌ 默认不补 | 跳过定标和 QUAC 两步，波长本来就没有 |

### 补充细节

1. **只影响 full 模式的多光谱**：只有走"定标 → QUAC"链路的 4 波段多光谱（GF-1 `-MSS`、GF-2 `-MSS1/-MSS2`、GF-6 `-MUX`、GF-7 `-BWDMUX`）会触发补波长这一步；PAN 全色和 geo 模式都不涉及。
2. **geo 模式想要波长也可以补**：如果老数据（geo 模式）产品后面想用于定量分析，可从**同区域 full 模式景的 `.hdr`** 复制 `wavelength` 字段（就那 4 行文本）——中心波长是传感器属性，同卫星同相机固定不变。
3. **如何判断一个文件缺波长**：看 `.hdr` 里有没有 `wavelength = {...}` 这一行；或用 `inspect_image.py` 抽检，它直接打印波长信息（如 `波长: 485, 555, 660, 830 nm`），没有即缺。

## 四、脚本实现（幂等追加）

批量脚本用 `ensure_wavelength()` 实现"**已有波长就跳过，没有就追加**"，可安全重复调用：

```python
def ensure_wavelength(hdr_path):
    """定标产物 .hdr 补波长；已存在则跳过（幂等）。"""
    with open(hdr_path, "r", encoding="utf-8", errors="ignore") as f:
        content = f.read()
    if "wavelength" in content.lower():
        return
    block = ("\nwavelength = {\n " + ", ".join(f"{w:.6f}" for w in WAVELENGTH) +
             "}\nwavelength units = Nanometers\n")
    with open(hdr_path, "a", encoding="utf-8") as f:
        f.write(block)
    log(f"[波长] 已写入 {hdr_path}")
```

其中 `WAVELENGTH = [485.0, 555.0, 660.0, 830.0]`（脚本顶部配置区定义）。

调用位置：辐射定标完成后、QUAC 执行前：

```python
# ① 辐射定标 → ①' 补波长 → ② QUAC
mux_cal = os.path.join(out, "mux_cal.dat")
...  # ApplyGainOffset 执行
ensure_wavelength(mux_cal.replace(".dat", ".hdr"))   # ①'
...  # QUAC 执行
```

## 五、相关踩坑索引

- 踩坑 #2：定标丢波长 → hdr 补 2 行，否则 QUAC 报"没有有效波长信息"（见 `MASTER_GUIDE.md` 第十一节）
- geo 模式补波长方法：见 `GF2_PROCESSING_GUIDE.md` 第八节建议 2
